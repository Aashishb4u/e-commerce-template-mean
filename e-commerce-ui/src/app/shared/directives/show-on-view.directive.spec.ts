import { ShowOnViewDirective } from './show-on-view.directive';

describe('ShowOnViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnViewDirective();
    expect(directive).toBeTruthy();
  });
});
